let line = 'Silly Sam sat on the sand';
let frontDoorLetter = line.charAt(0);
let frontDoorWord = 'shire';
let frontDoorPassword = frontDoorWord.charAt(0).toUpperCase() + frontDoorWord.slice(1);

let lineWithWhitespace = 'The quick brown fox jumps over the lazy dogs';
let backDoorLetter = 's';
let backDoorWord = 'horse';
let backDoorPassword = backDoorWord.charAt(0).toUpperCase() + backDoorWord.slice(1) + ', please';

/*let line = "Stands so high"
  let frontDoorLetter = line(0)
  
  let frontDoorWord = "SHIRE"
  let frontDoorPassword = frontDoorWord[0].toUpperCase() * frontDoorWord.slice(1.5).toLowerCase()

  LineWithWhitespace = "Reins and harness"
  let backDoorLetter = lineWithWhitespace.trim()
  let backDoorLetter = removeWhitespace[removeWhitespace.length -1]
  let backDoorWord = "Horse";
  let backDoorPassword = backDoorWord + ", please"
  */
